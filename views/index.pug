html
  head
    meta(property="og:image" content=imageUrl)
    meta(name="description" content=content)
    title= title
  body
    h1= content
    img(src=imageUrl)
    //- facebook sdk
    script(async defer crossorigin="anonymous"
      src="https://connect.facebook.net/en_US/sdk.js#xfbml=1&version=v5.0&appId=547910102421338&autoLogAppEvents=1")
    script.
     // facebook配置
     window.onload = () => {
      FB.ui({
        method: 'share',
        href: `https://testfb.hzmra.com/?type=live&id=205695763420807168` //这里换成你的网址
      }, function(response){
        console.log('分享回调', response)
        //分享回调
      })
     }
